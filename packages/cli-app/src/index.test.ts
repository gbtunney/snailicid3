import { describe, expect, test } from 'vitest'

describe('@snailicid3/cli-app', () => {
    test('placeholder — replace with real tests', () => {
        expect(true).toBe(true)
    })
})

export {}
