## API Report File for "@snailicid3/workspace"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { CommandResult } from '@snailicid3/node-utils';
import { EnvironmentDefinition } from '@snailicid3/node-utils';
import { RunCommandOptions } from '@snailicid3/node-utils';
import { StringClassifiers } from '@snailicid3/node-utils';
import { z } from 'zod';

// @public
export const BASE_WORKSPACE_SCOPES: readonly ["root"];

// @public
export type BaseRelation = 'ahead' | 'behind' | 'diverged' | 'equal' | 'unknown';

// @public
export type BaseSyncStatus = 'ahead' | 'behind' | 'diverged' | 'in-sync' | 'unknown';

// @public (undocumented)
export type BranchActionKind =
/** Only when genuinely impossible (detached HEAD, publish off base). */
'block'
/** Create the target branch from base. */
| 'create'
/** Already on the right branch; keep going. */
| 'proceed'
/** Relink: switch to the already-existing target branch. */
| 'switch';

// @public (undocumented)
export type BranchDecision = {
    action: BranchActionKind;
    carriesDirtyChanges: boolean;
    offerUpdateWithBase: boolean;
    reason: string;
    targetBranch: string;
    warnings: Array<string>;
};

// @public
export type BranchOperation = 'continue' | 'publish' | 'start';

// @public
export type BranchPrefix = 'changeset' | 'release';

// @public
export type BranchState = {
    baseBranch: string;
    baseSync: BaseSyncStatus;
    currentBranch: string;
    currentKind: CurrentBranchKind;
    onTarget: boolean;
    targetBranch: string;
    targetExistsLocal: boolean;
    targetExistsRemote: boolean;
    workingTree: WorkingTreeStatus;
};

// @public (undocumented)
export function checkProtectedBranch(repoRoot: string, operation: 'commit' | 'push', environment?: NodeJS.ProcessEnv): void;

// @public (undocumented)
export function checkStagedFilenames(repoRoot: string): void;

// @public
export const classifyBranchKind: (branch: string, baseBranch: string) => CurrentBranchKind;

// @public
export const createBranchFromBase: (repoRoot: string, branch: string, startPoint: string, options?: {
    inherit?: boolean;
}) => void;

// @public
export const createRepositoryScopeClassifiers: (snapshot: WorkspaceSnapshot, customClassifiers?: RepositoryScopeClassifiers, keepPrefix?: boolean) => RepositoryScopeClassifiers;

// @public
export type CurrentBranchKind = 'base' | 'changeset' | 'other' | 'release';

// @public
export const decideBranchAction: (state: BranchState, operation: BranchOperation) => BranchDecision;

// @public
export const deriveCommitFromBranch: (branch: string, options: {
    append?: string;
    scope: string;
}) => DerivedCommit | undefined;

// @public (undocumented)
export type DerivedCommit = {
    message: string;
    subject: string;
    type: BranchPrefix;
};

// @public
export function detectDefaultBranch(repoRoot: string): string;

// @public
export const executeBranchAction: (repoRoot: string, decision: BranchDecision, state: BranchState, options?: ExecuteBranchOptions) => void;

// @public
export type ExecuteBranchOptions = {
    inherit?: boolean;
    updateBase?: boolean;
};

// @public
export function findNearestPackageJson(repoRoot: string, inputPath: string): null | string;

// @public
export function formatScopes(scopes: ReadonlyArray<string>, format: ScopeFormat): string;

// @public
export const formatWorkflowPlan: (plan: WorkflowPlan) => string;

// @public
export const gatherBranchState: (repoRoot: string, options: {
    baseBranch: string;
    slug: string;
    targetPrefix: BranchPrefix;
}) => BranchState;

// @public
export const gatherWorkflowFacts: (repoRoot: string, options: {
    baseBranch: string;
}) => WorkflowFacts;

// @public
export const getBaseRelation: (repoRoot: string, baseBranch: string) => {
    aheadCount: number;
    behindCount: number;
    relation: BaseRelation;
};

// @public
export const getBaseSyncStatus: (repoRoot: string, baseBranch: string) => BaseSyncStatus;

// @public (undocumented)
export function getCurrentBranch(repoRoot: string): string;

// @public
export function getGitChangedFiles(options?: GitChangedFilesOptions): Array<string>;

// @public
export function getGitChangedFilesByArea(options?: GitChangedFilesOptions): Array<GitChangedFile>;

// @public
export function getPackageBinaryCommand(packageManager: PackageManager, binary: string, args?: ReadonlyArray<string>): PackageCommand;

// @public
export function getPackageManager(repoRoot: string, environment?: NodeJS.ProcessEnv): PackageManager;

// @public
export function getPackageScriptCommand(packageManager: PackageManager, script: string, args?: ReadonlyArray<string>): PackageCommand;

// @public
export function getRepoRoot(options?: {
    fallbackToCwd?: boolean;
}): string;

// @public
export const getWorkingTreeStatus: (repoRoot: string) => WorkingTreeStatus;

// @public
export function getWorkspacePackagesList(filter?: (pkg: WorkspacePackage) => boolean, repoRoot?: string): Array<WorkspacePackage>;

// @public
export const getWorkspaceScopeClassifiers: (snapshot: WorkspaceSnapshot, keepPrefix?: boolean) => RepositoryScopeClassifiers;

// @public
export function getWorkspaceScopes(options: {
    keepPrefix?: boolean;
    overrides?: WorkspaceScopeOverrides;
    snapshot: WorkspaceSnapshot;
    standardScopes?: Readonly<Record<string, ReadonlyArray<string>>>;
}): ResolvedWorkspaceScopes;

// @public
export function getWorkspaceSnapshot(repoRoot?: string): WorkspaceSnapshot;

// @public
export type GitChangeArea = 'range' | 'staged' | 'unstaged' | 'untracked';

// @public
export type GitChangedFile = {
    areas: Array<GitChangeArea>;
    file: string;
};

// @public (undocumented)
export type GitChangedFilesOptions = {
    base?: string;
    cwd?: string;
    head?: string;
    includeStaged?: boolean;
    includeUnstaged?: boolean;
    includeUntracked?: boolean;
};

// @public
export const inferWorkflowIdentity: (branch: string) => undefined | WorkflowIdentity;

// @public
export const isWorkflowBranch: (branch: string) => boolean;

// @public
export const listWorkflowBranches: (repoRoot: string) => WorkflowBranches;

// @public
export function loadScopePathMatchers(repoRoot: string): Promise<WorkspaceScopeOverrides>;

// @public
export const localBranchExists: (repoRoot: string, branch: string) => boolean;

// @public (undocumented)
export type PackageCommand = {
    args: Array<string>;
    command: PackageManager;
};

// @public (undocumented)
export type PackageManager = 'npm' | 'pnpm';

// @public (undocumented)
export type PackageManagerResolution = {
    packageManager: PackageManager;
    source: 'default' | 'environment' | 'package.json';
};

// @public
export const parseBranchName: (branch: string) => ParsedBranchName | undefined;

// @public (undocumented)
export type ParsedBranchName = {
    prefix: BranchPrefix;
    slug: string;
};

// @public
export const planWorkflow: (facts: WorkflowFacts) => WorkflowPlan;

// @public
export function readPackageName(packageJsonPath: string): null | string;

// @public
export const readWorkspaceEnvironment: (environment: Readonly<Record<string, unknown>>) => WorkspaceEnvironment;

// @public
export const remoteBranchExists: (repoRoot: string, branch: string) => boolean;

// @public (undocumented)
export type RepositoryScopeClassifiers = StringClassifiers;

// @public (undocumented)
export type RepositoryScopeResolution = {
    matches: Readonly<Record<string, Array<string>>>;
    scopes: Array<string>;
    unmatched: Array<string>;
};

// @public
export function resolveBaseBranch(repoRoot: string, ...preferred: ReadonlyArray<string | undefined>): string;

// @public
export type ResolvedWorkspaceScopes = {
    classifiers: RepositoryScopeClassifiers;
    definitions: WorkspaceScopes;
    names: Array<string>;
    sources: Readonly<Record<string, Array<WorkspaceScopeSource>>>;
};

// @public
export function resolvePackageManager(repoRoot: string, environment?: NodeJS.ProcessEnv): PackageManagerResolution;

// @public
export const resolveRepositoryScopes: (files: ReadonlyArray<string>, classifiers: RepositoryScopeClassifiers) => RepositoryScopeResolution;

// @public (undocumented)
export function runCommitMessageHook(repoRoot: string, messageFile: string): void;

// @public (undocumented)
export function runLintStaged(repoRoot: string, environment?: NodeJS.ProcessEnv): void;

// @public (undocumented)
export function runPackageBinary(repoRoot: string, binary: string, args?: ReadonlyArray<string>, options?: RunCommandOptions): CommandResult;

// @public
export function runPackageManager(repoRoot: string, args?: ReadonlyArray<string>, options?: RunCommandOptions): CommandResult;

// @public (undocumented)
export function runPackageScript(repoRoot: string, script: string, args?: ReadonlyArray<string>, options?: RunCommandOptions): CommandResult;

// @public (undocumented)
export function runSnailSh(repoRoot: string, command: string, args?: ReadonlyArray<string>): void;

// @public
export function safeGetWorkspaceSnapshot(repoRoot?: string): WorkspaceSnapshotResult;

// @public (undocumented)
export type ScopeFormat = 'csv' | 'list';

// @public
export function shortenScopeName(scopeName: string, keepPrefix?: boolean): string;

// @public (undocumented)
export function showHookSection(repoRoot: string, hookName: string): void;

// @public
export const SNAILICID3_COMMITLINT_CONFIG_KEY: "snailicid3";

// @public
export const STANDARD_WORKSPACE_SCOPES: {
    readonly actions: readonly [".github/workflows/**", ".github/actions/**", ".github/scripts/**"];
    readonly notes: readonly ["notes/**"];
    readonly scripts: readonly ["scripts/**"];
};

// @public
export const switchBranch: (repoRoot: string, branch: string, options?: {
    inherit?: boolean;
}) => void;

// @public (undocumented)
export function validateBranchName(repoRoot: string): void;

// @public (undocumented)
export type WorkflowActionId = 'commit' | 'stage' | 'start-changeset' | 'switch-workflow-branch' | 'update-base';

// @public (undocumented)
export type WorkflowBranches = {
    local: Array<string>;
    remote: Array<string>;
};

// @public
export type WorkflowFacts = {
    aheadCount: number;
    baseBranch: string;
    baseRelation: BaseRelation;
    behindCount: number;
    currentBranch: string;
    detached: boolean;
    identity: undefined | WorkflowIdentity;
    matchingBranches: WorkflowBranches;
    stagedFiles: Array<string>;
    workflowBranches: WorkflowBranches;
    workingTree: 'clean' | 'dirty';
};

// @public
export type WorkflowIdentity = {
    branch: string;
    mode: BranchPrefix;
    slug: string;
};

// @public
export type WorkflowNextAction = {
    command: string;
    consequence: string;
    id: WorkflowActionId;
};

// @public (undocumented)
export type WorkflowPlan = {
    facts: WorkflowFacts;
    nextActions: Array<WorkflowNextAction>;
    stacked: boolean;
    unavailableReason: string | undefined;
    warnings: Array<string>;
};

// @public (undocumented)
export type WorkingTreeStatus = 'clean' | 'dirty';

// @public (undocumented)
export type WorkspaceEnvironment = z.output<typeof workspaceEnvironment.schema>;

// @public
export const workspaceEnvironment: EnvironmentDefinition<    {
readonly allowDirty: z.ZodType<boolean, unknown, z.core.$ZodTypeInternals<boolean, unknown>>;
readonly baseBranch: z.ZodDefault<z.ZodString>;
readonly commandName: z.ZodDefault<z.ZodString>;
readonly logging: z.ZodType<boolean, unknown, z.core.$ZodTypeInternals<boolean, unknown>>;
readonly packageManagerOverride: z.ZodOptional<z.ZodEnum<{
npm: "npm";
pnpm: "pnpm";
}>>;
readonly patchCwd: z.ZodDefault<z.ZodString>;
readonly prefix: z.ZodDefault<z.ZodString>;
readonly prefixOverride: z.ZodDefault<z.ZodString>;
readonly protectedBranches: z.ZodType<string[], unknown, z.core.$ZodTypeInternals<string[], unknown>>;
readonly skipCommitlint: z.ZodType<boolean, unknown, z.core.$ZodTypeInternals<boolean, unknown>>;
readonly skipLintStaged: z.ZodType<boolean, unknown, z.core.$ZodTypeInternals<boolean, unknown>>;
}>;

// @public (undocumented)
export type WorkspacePackage = {
    name: string;
    path: string;
    private?: boolean;
    version: string;
};

// @public
export const workspacePackageManagerOutputSchema: (repoRoot: string) => z.ZodType<Array<WorkspacePackage>, string>;

// @public
export const workspacePackageRecordSchema: (repoRoot: string) => z.ZodType<WorkspacePackage>;

// @public
export type WorkspaceScopeDefinition = ReadonlyArray<string> | true;

// @public
export type WorkspaceScopeOverride = boolean | ReadonlyArray<string> | undefined;

// @public (undocumented)
export type WorkspaceScopeOverrides = Readonly<Record<string, WorkspaceScopeOverride>>;

// @public
export const workspaceScopeOverridesSchema: z.ZodRecord<z.ZodString, z.ZodOptional<z.ZodUnion<readonly [z.ZodBoolean, z.ZodArray<z.ZodString>]>>>;

// @public (undocumented)
export type WorkspaceScopes = Readonly<Record<string, WorkspaceScopeDefinition>>;

// @public
export type WorkspaceScopeSource = 'override' | 'package' | 'standard';

// @public
export type WorkspaceSnapshot = {
    list: Array<WorkspacePackage>;
    lookup: Map<string, WorkspacePackage>;
    repoRoot: string;
};

// @public (undocumented)
export type WorkspaceSnapshotResult = {
    data: WorkspaceSnapshot;
    success: true;
} | {
    error: string;
    success: false;
};

// (No @packageDocumentation comment for this package)

```
