{
    "extends": "@snailicid3/config/tsconfig-base",
    "compilerOptions": {
        "rootDir": ".",
        "outDir": "../dist",
        "declarationDir": "../types",
        "sourceMap": true,
        "declaration": true,
        "declarationMap": true,
        "lib": ["ESNext", "DOM"],
        "types": ["node"]
    },
    "include": ["**/*.ts", "**/*.cts", "**/*.mts"],
    /** 
    * TODO fix this in all packages so 
    tests are compiled and also depend on build:ts to catch errors */
    "exclude": [
        "**/*.test.ts",
        "**/*.test.mts",
        "**/*.test.cts",
        "**/*.spec.ts",
        "**/*.spec.mts",
        "**/*.spec.cts"
    ]
}
