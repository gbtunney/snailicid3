## API Report File for "@snailicid3/color"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import * as _chroma from 'chroma.ts';
import { Chromable } from 'chroma.ts';
import { Color } from 'chroma.ts';
import { ColorFormat } from 'chroma.ts';
import ColorIO from 'colorjs.io';
import { ColorMode } from 'chroma.ts';
import { ColorObject } from 'colorjs.io';
import { Coords } from 'colorjs.io';
import { Scale } from 'chroma.ts';

// @public (undocumented)
const analogous: (color: Chromable, results?: number, slices?: number) => Array<Color>;

// @public (undocumented)
export const apcaContrast: (theme: ColorTheme) => number;

// @public (undocumented)
export function assertHexColor(value: string, ctx?: string): asserts value is HexColor;

// Warning: (ae-internal-missing-underscore) The name "Chroma" should be prefixed with an underscore because the declaration is marked as @internal
//
// @internal (undocumented)
export const Chroma: typeof _chroma;

export { Chromable }

// @public (undocumented)
type ChromaColorPalatte = {
    analogous: Array<Color>;
    chroma: Color;
    complement: Color;
    hue: number;
    lightness: number;
    luminance: number;
    saturation: number;
    split_complement: Array<Color>;
    temperature: number;
    tetrad: Array<Color>;
    textColor: Color;
    triad: Array<Color>;
};

export { Color }

export { ColorFormat }

// @public (undocumented)
export type ColorJS = ColorIO;

export { ColorMode }

// @public (undocumented)
export type ColorTheme = {
    bg: ColorJS | HexColor;
    fg: ColorJS | HexColor;
};

declare namespace colorUtils {
    export {
        isValidColor_2 as isValidColor,
        getChromaColor,
        getColor,
        complement,
        triad,
        tetrad,
        splitComplement,
        analogous,
        ChromaColorPalatte
    }
}

// @public
const complement: (color: Chromable, format?: ColorFormat) => Color;

// @public
export type CSSColorSpecial = 'currentColor' | 'currentcolor' | 'inherit' | 'initial' | 'revert' | 'transparent' | 'unset';

// @public (undocumented)
const getChromaColor: (value: Chromable) => Color | undefined;

// @public (undocumented)
const getColor: (value: Chromable, format?: ColorFormat) => Color | undefined;

// @public (undocumented)
export type HexColor = `#${string}` & {
    readonly __hexBrand: 'HexColor';
};

// @public (undocumented)
export const isHexColor: (value: string) => value is HexColor;

// @public
export function isValidColor(input: string): boolean;

// @public
const isValidColor_2: <Type extends Chromable>(value: Type) => value is Type;

// @public (undocumented)
export function mapColorJSCoords(color: ColorJS, mapFunction: (value: number) => number): [number, number, number];

// @public (undocumented)
export const normalizeRGBCoords: (color: ColorJS) => Coords;

// @public
export function parseColorJS(input: ColorObject | string, ctx?: string): ColorJS;

// @public (undocumented)
export const parseColorToHex: typeof parseColorToHexStrict;

// @public
export function parseColorToHexStrict(input: ColorObject | string, ctx?: string): HexColor;

// @public (undocumented)
export function readableTextHex(color: ColorJS | string, theme?: keyof ColorTheme): 'black' | 'white';

export { Scale }

// @public (undocumented)
const splitComplement: (color: Chromable, format?: ColorFormat) => Array<Color>;

// @public (undocumented)
const tetrad: (color: Chromable, format?: ColorFormat) => Array<Color>;

// @public (undocumented)
export function toHex(color: ColorJS, includeAlpha?: boolean): string;

// @public (undocumented)
const triad: (color: Chromable, format?: ColorFormat) => Array<Color>;

// @public
export function tryParseColorToHex(input: string): undefined;

// (No @packageDocumentation comment for this package)

```
